#include <stdio.h>
/**
 * main - main block
 * Description: Print all the numbers of base 10 from 0.
 * Return: 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 10; i++)
		printf("%i\n", i);
	return (0);
}
