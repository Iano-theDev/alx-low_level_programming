#include <stdio.h>

void puts_half(char *);

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "Lorem";
	puts_half(str);
	return (0);
}
