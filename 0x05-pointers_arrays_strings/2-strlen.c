#include <stdio.h>
/**
 * _strlen - gets string length
 * @s: string character
 * Return: length of string
 */

int _strlen(char *s)
{
	int len = 0;

	while (*s != '\0')
	{
		s++;
		l++;
	}
	return (len);
}
