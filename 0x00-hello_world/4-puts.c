#include <stdio.h>

/**
 * main - Entry point, main block.
 * Description: Uses puts to print a string in stdio
 * Return: 0 always.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
