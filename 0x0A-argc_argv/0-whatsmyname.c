#include "main.h"

/**
 * main - preints the name of the program
 * @argc: arguments count to main
 * @argv: argument vector array, all arguments of size argc
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
